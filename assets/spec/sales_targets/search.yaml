/salestarget/search:
  post:
    tags:
      - salestargets
    description: search.
    parameters:
      - name: body
        in: body
        schema:
          type: object
          properties:
            data:
              type: object
              properties:
                inventlocationid:
                  type: string
                  example: "RMAW-0010"

    responses:
      200:
        description: "status:1 is success; status: 0 is failure;"
